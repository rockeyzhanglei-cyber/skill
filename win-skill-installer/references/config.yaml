# WinAi Skills 安装配置
tfs:
  base_url: "http://tfs2018-web.winning.com.cn:8080/tfs"
  collection: "WinCode"
  skill_project: "Skill"
  mcp_project: "MCP"
  host: "tfs2018-web.winning.com.cn:8080"

cache:
  root: "~/.cache/WinCode"
  skills_dir: "skill"
  mcp_dir: "mcp"
  registry: "registry.json"

install:
  target_dir: "~/.claude/skills"
  mode: "symlink"
